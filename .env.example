# Claude Code Dashboard Environment Configuration

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Authentication (set DISABLE_AUTH=true for development)
DISABLE_AUTH=false
SECRET_KEY=your-secret-key-change-in-production-use-strong-random-string
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Default Admin Credentials
DASHBOARD_USERNAME=admin
DASHBOARD_PASSWORD=claude-dashboard

# Frontend Configuration (for Docker builds)
VITE_API_URL=http://localhost:8000/api

# Development Settings
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Logging
LOG_LEVEL=INFO

# Instance Configuration
DEFAULT_CLAUDE_PORT=8000
HEALTH_CHECK_TIMEOUT=30

# Docker Settings
DOCKER_HOST=unix:///var/run/docker.sock

# Production Notes:
# 1. Change SECRET_KEY to a strong random string
# 2. Change default admin credentials
# 3. Set DISABLE_AUTH=false for production
# 4. Update VITE_API_URL to your production domain
# 5. Set appropriate CORS_ORIGINS for production